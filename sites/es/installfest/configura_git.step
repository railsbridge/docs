message "(Si utilizaste el Instalador de Rails en Windows ya deberías tener `user.name` y `user.email` configurados.)"

console <<-BASH
git config --global user.name "Tu nombre"
git config --global user.email "Tu e-mail"
BASH

important "Utiliza el mismo e-mail para Heroku, Git, GitHub, y SSH."

verify do
  console "git config --get user.name"
  result "Tu nombre"
  console "git config --get user.email"
  result "Tu e-mail"
end

tip "Texto de salida de Git en colores" do
  console <<-BASH
git config --global color.diff auto
git config --global color.status auto
git config --global color.branch auto
  BASH

  important do
    message "Si estás utilizando Windows, posiblemente vas a tener que instalar AnsiCon para ver los colores (de otra manera vas a ver texto sin sentido). Si no hiciste esto anteriormente, deberás hacerlo en este momento."

    link_without_toc "colores_de_windows"
  end
end
